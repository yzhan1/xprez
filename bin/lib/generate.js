export default (targetName, cmd) => {
  if (!cmd.controller && !cmd.service && !cmd.view) {
    // generate full project

  } else {
    if (cmd.controller) {
      // generate controller

    }
    if (cmd.service) {
      // generate service

    }
    if (cmd.view) {
      // generate view

    }
  }
};
